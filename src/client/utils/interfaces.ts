export interface IServices {
    id?: number,
    name?: string,
    description?: string,
    icon?: any,
    
}

export interface IPerson {
    name?: string,
    description?: string,
    image?: string,
}