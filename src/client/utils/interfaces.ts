export interface IServices {
    id?: number,
    name?: string,
    description?: string,
    icon?: any,
    
}

export interface IPeople {
    person?: string,
    about?: string,
    image?: string,
}